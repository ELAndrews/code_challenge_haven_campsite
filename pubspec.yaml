name: haven_campsite
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  bloc: ^7.1.0
  provider: ^5.0.0
  flutter_bloc: ^7.1.0
  directed_graph: ^0.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - lib/assets/images/
